ripple-test {
  metadata-table = "test-metadata"
  fact-table = "sessions"
  resource-bucket = "test-resource-bucket"
  aws-region = "cn-north-1"
  time-zone = "Asia/Shanghai"
  listen-internal = 1000
}